/** The stored reason, in the words the confirm panel offered. Shared between
 *  ArchivePanel's confirm flow and the account screen's archived-pets card,
 *  so the two say the same thing. */
export const REASONS = {
  passed_away: "Passed away",
  rehomed: "Rehomed",
  other: "Something else",
};
